export type Severity = 'normal' | 'alert';
export interface ArticleLocation { locationId: string; nameRu: string; nameTg: string; locationType: 'region' | 'district' | 'city' | 'town'; longitude: number | null; latitude: number | null; regionId: string | null; districtId: string | null; confidence: number; evidence: string; evidenceField: 'title' | 'description'; matchedAlias: string; method: 'deterministic_alias' | 'ai_disambiguation'; aiReason?: string; }
export interface NewsItem { id: string; title: string; description: string; url: string; sourceId: string; sourceName: string; category: string; imageUrl?: string; imageAlt?: string; categoryConfidence?: number; importance?: 'info' | 'important' | 'warning' | 'critical'; importanceConfidence?: number; enrichmentStatus?: 'pending' | 'rules_only' | 'completed' | 'review_required' | 'failed'; publishedAt: string; severity: Severity; locations?: ArticleLocation[]; geolocationCandidates?: ArticleLocation[]; geolocationStatus?: 'resolved' | 'review_required' | 'ai_resolved' | 'ai_failed'; geolocationThreshold?: number; }
export interface SourceStatus { id: string; name: string; status: 'online' | 'degraded' | 'offline'; count: number; checkedAt: string; error?: string; }
